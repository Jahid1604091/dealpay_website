const nextConfig = {
  // output: 'export',
  // distDir:'out',

    images: {
      unoptimized:true,
      domains: ['dealpay-asia.glitch.me'],
    },
    // trailingSlash: true,
  };
  
  export default nextConfig